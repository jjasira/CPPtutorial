#include <iostream>
using namespace std;

int main()
{
    // to declare more than one variable of the same type, use a comma-separated list
    int myNum = 15, myNum2 = 11;
    int sum = myNum + myNum2;
    cout << "I am " << sum << " years old";
    return 0;
}